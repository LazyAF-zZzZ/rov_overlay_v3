<UserControl x:Class="RovOverlay.Desktop.Views.Controls.TeamEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:svc="clr-namespace:RovOverlay.Desktop.Services"
             xmlns:ctl="clr-namespace:RovOverlay.Desktop.Views.Controls">
    <!-- DataContext: a TeamEditor -->
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Style="{StaticResource FieldLabel}" Text="{svc:T Team.Name}"/>
                <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" MaxLength="24"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Style="{StaticResource FieldLabel}" Text="{svc:T Team.Tag}"/>
                <TextBox Text="{Binding Tag, UpdateSourceTrigger=PropertyChanged}" MaxLength="6"/>
            </StackPanel>
            <StackPanel Grid.Column="3" Width="90" Margin="10,0,0,0" Visibility="{Binding ShowSeed, Converter={StaticResource BoolToVis}}">
                <TextBlock Style="{StaticResource FieldLabel}" Text="{svc:T Team.SeedLabel}"/>
                <TextBox Text="{Binding Seed, UpdateSourceTrigger=PropertyChanged}" MaxLength="4" HorizontalContentAlignment="Center"/>
            </StackPanel>
        </Grid>

        <TextBlock Style="{StaticResource FieldLabel}" Text="{svc:T Team.PlayersLabel}" Margin="0,12,0,6"/>
        <ctl:PlayerEditorView DataContext="{Binding Players}"/>

        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
            <Button Command="{Binding UploadLogoCommand}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Style="{StaticResource Icon}" Text="&#xEB9F;" FontSize="12" Margin="0,0,7,0"/>
                    <TextBlock Text="{svc:T Team.UploadLogo}"/>
                </StackPanel>
            </Button>
            <Button Content="{svc:T Team.ClearLogo}" Command="{Binding ClearLogoCommand}" Margin="8,0,0,0"/>
            <TextBlock Text="{svc:T Team.LogoHint}" Foreground="{StaticResource MutedBrush}" FontSize="11"
                       VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>

        <DockPanel Margin="0,14,0,0" LastChildFill="False">
            <Button DockPanel.Dock="Right" Style="{StaticResource DangerButton}" Content="{svc:T Team.DeleteFromRegistry}"
                    Command="{Binding DeleteCommand}"/>
            <Button Style="{StaticResource PrimaryButton}" Content="{svc:T Team.SaveTeam}" Command="{Binding SaveCommand}" MinWidth="100"/>
        </DockPanel>
    </StackPanel>
</UserControl>
